// Shows a grid on the ground plane
const grid = new THREE.GridHelper(10, 10, 0x888888, 0x555555);
scene.add(grid);

// Position
// grid.position.set(0, 0, 0);
// grid.position.y = -1;  // Move down

// Rotation
// grid.rotation.x = Math.PI / 2;  // Rotate to vertical plane
// grid.rotation.y = Math.PI / 4;  // Rotate 45 degrees

// Scale
// grid.scale.set(2, 2, 2);  // Make bigger
// grid.scale.x = 0.5;  // Stretch/compress

// Visibility
// grid.visible = false;  // Hide
// grid.visible = true;   // Show

// Material properties
// grid.material.opacity = 0.5;
// grid.material.transparent = true;
