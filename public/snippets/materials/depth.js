// A material that renders geometry based on its distance from the camera.
// Nearer objects are darker, farther objects are lighter.
// Useful for depth-based visual effects like fog.
const material = new THREE.MeshDepthMaterial();