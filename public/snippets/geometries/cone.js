// A cone shape.
// ConeGeometry(radius, height, radialSegments)
const radius = 1;
const height = 2;
const radialSegments = 16; // Number of segments around the cone.
const geometry = new THREE.ConeGeometry(radius, height, radialSegments);